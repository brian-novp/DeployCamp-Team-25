name: Push to DockerHub

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    # checkout the repo
    - name: 'Checkout Github Action'
      uses: actions/checkout@master

    - name: login to docker registry
      uses: docker/login-action@v3
      with:
        username: ${{secrets.DOCKERHUB_USERNAME}}
        password: ${{secrets.DOCKERHUB_TOKEN}}

    - name: build and push docker image to registry
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{secrets.DOCKERHUB_USERNAME}}/devcamp25:${{ github.sha }}

    - name: replace docker-imgtag inside docker-compose.yaml
      uses: jacobtomlinson/gha-find-replace@v3
      with:
        find: "docker-imgtag"
        replace: ${{ github.sha }}

    - name: Read user-script.sh
      id: package
      uses: juliangruber/read-file-action@v1
      with:
        path: ./docker-compose.yaml

    - name: Echo user-script.sh
      run: echo "${{ steps.package.outputs.content }}"

    # - name: scp docker-compose.yaml to biznet vm
    #   uses: appleboy/scp-action@v1
    #   with:
    #     host: ${{ secrets.HOST }}
    #     username: ${{ secrets.USERNAME }}
    #     key: ${{ secrets.KEY }}
    #     port: ${{ secrets.PORT }}
    #     source: ./docker-compose.yaml
    #     target: /home/debian/devcamp