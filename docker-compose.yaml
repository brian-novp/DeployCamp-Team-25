services:
  audience-service:
    image: docker.io/ahmeeddhon/devcamp25:docker-imgtag
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - 8000:8000
    restart: always